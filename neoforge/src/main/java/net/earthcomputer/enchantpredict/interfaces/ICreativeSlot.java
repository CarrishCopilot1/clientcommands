package net.earthcomputer.enchantpredict.interfaces;

/**
 * Duck-type interface attached by the creative slot mixin so the item throw
 * logic can skip creative-only slots. Matches the original {@code ICreativeSlot}.
 */
public interface ICreativeSlot {
}
